import { useState } from 'react';
import { Link } from 'wouter';
import { Button } from '@/components/ui/button';
import { Check, Star, Download } from 'lucide-react';

export default function PresetBundles() {
  const [selectedBundle, setSelectedBundle] = useState('all');

  const presets = [
    {
      id: 'bare',
      name: 'Bare Bundle',
      price: 5,
      description: 'Clean, minimal editing for authentic, natural looks',
      previewImage: '/api/placeholder/300/400',
      features: ['5 Lightroom Presets', 'Mobile & Desktop Compatible', 'Video Tutorial Included']
    },
    {
      id: 'glow',
      name: 'Glow Bundle', 
      price: 5,
      description: 'Warm, luminous tones for that perfect golden hour glow',
      previewImage: '/api/placeholder/300/400',
      features: ['5 Lightroom Presets', 'Mobile & Desktop Compatible', 'Video Tutorial Included']
    },
    {
      id: 'skin',
      name: 'Skin Bundle',
      price: 5,
      description: 'Perfect skin tones and smooth complexion enhancement',
      previewImage: '/api/placeholder/300/400',
      features: ['5 Lightroom Presets', 'Mobile & Desktop Compatible', 'Video Tutorial Included']
    },
    {
      id: 'selfie-glow',
      name: 'Selfie Glow-Up Bundle',
      price: 5,
      description: 'The ultimate selfie enhancement collection',
      previewImage: '/api/placeholder/300/400',
      features: ['8 Lightroom Presets', 'Mobile & Desktop Compatible', 'Bonus Editing Guide']
    }
  ];

  const testimonials = [
    {
      name: "Rachel Green",
      content: "These presets completely transformed my Instagram aesthetic. My engagement has tripled!",
      rating: 5
    },
    {
      name: "Sophia Martinez", 
      content: "The Glow Bundle is pure magic. Every photo looks like it was taken by a professional.",
      rating: 5
    },
    {
      name: "Ashley Chen",
      content: "I've tried so many presets, but these are the only ones that actually work on every photo.",
      rating: 5
    }
  ];

  const totalValue = presets.reduce((sum, preset) => sum + preset.price, 0);
  const bundlePrice = 15;
  const savings = totalValue - bundlePrice;

  return (
    <div className="min-h-screen bg-white font-light">
      <nav className="border-b border-gray-200 px-6 py-4 font-sans">
        <div className="max-w-6xl mx-auto flex items-center justify-between">
          <Link href="/">
            <div className="text-xl font-medium tracking-wide">SELFIE AI™</div>
          </Link>
          <div className="flex items-center gap-4">
            <Link href="/temp-admin">
              <Button variant="outline" size="sm">Admin</Button>
            </Link>
          </div>
        </div>
      </nav>

      <section className="px-6 py-20">
        <div className="max-w-4xl mx-auto text-center">
          <h1 className="text-6xl mb-6 leading-tight font-serif">Lightroom Preset Collection</h1>
          <p className="text-xl text-[#4C4B4B] mb-10 max-w-3xl mx-auto leading-relaxed">
            Professional-grade presets designed to give your photos that signature editorial look in one click. Used by thousands of content creators worldwide.
          </p>
          <div className="flex items-center justify-center gap-2 mb-12">
            <div className="flex">
              {[...Array(5)].map((_, i) => (
                <Star key={i} className="w-5 h-5 fill-black text-black" />
              ))}
            </div>
            <span className="text-sm text-[#4C4B4B]">4.9/5 from 12,847 customers</span>
          </div>
        </div>
      </section>

      <section className="px-6 mb-20">
        <div className="max-w-4xl mx-auto">
          <div className="grid md:grid-cols-2 gap-6">
            <button
              onClick={() => setSelectedBundle('individual')}
              className={`p-6 border uppercase font-sans tracking-wide text-left transition-all ${selectedBundle === 'individual' ? 'border-black bg-gray-50' : 'border-gray-300 hover:border-black'}`}
            >
              <h3 className="text-xl mb-2">Individual Presets</h3>
              <p className="text-[#4C4B4B]">Choose your favorite bundles separately</p>
              <div className="mt-4 text-xl font-bold">$5 each</div>
            </button>
            <button
              onClick={() => setSelectedBundle('all')}
              className={`p-6 border uppercase font-sans tracking-wide text-left relative transition-all ${selectedBundle === 'all' ? 'border-black bg-gray-50' : 'border-gray-300 hover:border-black'}`}
            >
              <div className="absolute -top-3 -right-3 bg-black text-white px-3 py-1 text-xs">Save ${savings}</div>
              <h3 className="text-xl mb-2">Complete Collection</h3>
              <p className="text-[#4C4B4B]">All 4 preset bundles included</p>
              <div className="mt-4">
                <span className="text-gray-400 line-through">${totalValue}</span>
                <span className="text-xl font-bold ml-2">${bundlePrice}</span>
              </div>
            </button>
          </div>
        </div>
      </section>

      <section className="px-6 py-20 bg-[#F1F1F1]">
        <div className="max-w-6xl mx-auto">
          <h2 className="text-4xl text-center mb-12 font-serif">Choose Your Style</h2>
          <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
            {presets.map((preset) => (
              <div key={preset.id} className="bg-white border border-gray-200">
                <div className="aspect-[3/4] bg-gray-100 relative">
                  <img src={preset.previewImage} alt={preset.name} className="w-full h-full object-cover" />
                  <div className="absolute inset-0 bg-black bg-opacity-30 flex items-center justify-center opacity-0 hover:opacity-100 transition-opacity">
                    <span className="text-white text-sm">Preview</span>
                  </div>
                </div>
                <div className="p-6">
                  <h3 className="text-lg font-medium mb-1">{preset.name}</h3>
                  <p className="text-sm text-[#4C4B4B] mb-4">{preset.description}</p>
                  <ul className="space-y-1 text-sm mb-6 text-[#4C4B4B]">
                    {preset.features.map((feature, i) => (
                      <li key={i} className="flex items-center gap-2">
                        <Check className="w-4 h-4 text-black" /> {feature}
                      </li>
                    ))}
                  </ul>
                  {selectedBundle === 'individual' && (
                    <Link href={`/checkout/preset-${preset.id}`}>
                      <Button className="w-full bg-black text-white hover:bg-[#3C3A35] uppercase tracking-wide text-sm">
                        Buy Now - ${preset.price}
                      </Button>
                    </Link>
                  )}
                </div>
              </div>
            ))}
          </div>
          {selectedBundle === 'all' && (
            <div className="text-center mt-12">
              <Link href="/checkout/preset-bundle-all">
                <Button size="lg" className="bg-black text-white hover:bg-[#3C3A35] px-12 py-6 text-lg tracking-wide uppercase">
                  Get All Presets for ${bundlePrice}
                </Button>
              </Link>
            </div>
          )}
        </div>
      </section>

      <section className="px-6 py-20">
        <div className="max-w-4xl mx-auto">
          <h2 className="text-4xl font-serif text-center mb-12">See The Transformation</h2>
          <div className="grid md:grid-cols-2 gap-8">
            <div className="bg-gray-100 aspect-square" />
            <div className="bg-gray-100 aspect-square" />
          </div>
        </div>
      </section>

      <section className="px-6 py-20 bg-[#F1F1F1]">
        <div className="max-w-4xl mx-auto">
          <h2 className="text-4xl font-serif text-center mb-12">What Creators Are Saying</h2>
          <div className="grid md:grid-cols-3 gap-8">
            {testimonials.map((t, i) => (
              <div key={i} className="bg-white p-6">
                <div className="flex justify-center mb-4">
                  {[...Array(t.rating)].map((_, i) => (
                    <Star key={i} className="w-4 h-4 fill-yellow-400 text-yellow-400" />
                  ))}
                </div>
                <blockquote className="italic mb-4 text-[#4C4B4B]">"{t.content}"</blockquote>
                <p className="font-medium text-sm">{t.name}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      <section className="px-6 py-20 bg-black text-white">
        <div className="max-w-2xl mx-auto text-center">
          <h2 className="text-4xl font-serif mb-6">Transform Your Photos Today</h2>
          <p className="text-lg text-gray-300 mb-8">
            Join thousands of creators who've elevated their content with our presets.
          </p>
          <Link href="/checkout/preset-bundle-all">
            <Button size="lg" className="bg-white text-black hover:bg-gray-100 px-12 py-6 text-lg tracking-wide uppercase">
              Get Complete Collection - ${bundlePrice}
            </Button>
          </Link>
          <p className="text-sm text-gray-400 mt-4">
            ✓ Instant download • ✓ 30-day guarantee • ✓ Works with Lightroom & Lightroom Mobile
          </p>
        </div>
      </section>

      <footer className="px-6 py-12 border-t border-gray-200 text-center text-[#4C4B4B]">
        <p>© 2024 SELFIE AI™. All rights reserved.</p>
      </footer>
    </div>
  );
}
