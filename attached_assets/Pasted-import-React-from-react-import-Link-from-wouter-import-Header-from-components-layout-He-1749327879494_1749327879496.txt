import React from 'react';
import { Link } from "wouter";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { useQuery } from "@tanstack/react-query";

export default function PhotoVault() {
  const { data: user } = useQuery({
    queryKey: ["/api/me"],
    retry: false,
  });

  const vaultImages = [
    "https://i.postimg.cc/HLN6LDSj/15.png",
    "https://i.postimg.cc/Bbn4rXK7/3.png",
    "https://i.postimg.cc/nhDpf8Xx/35.png",
    "https://i.postimg.cc/26JSx8ss/5.png",
    "https://i.postimg.cc/dQ5q8mbb/22.png",
    "https://i.postimg.cc/V673b4J4/81.png"
  ];

  return (
    <div className="min-h-screen bg-[#F1F1F1]">
      <Header user={user} />

      {/* Studio Navigation */}
      <section className="w-full py-4 px-4 border-b border-[#E5E5E5] sticky top-0 bg-white z-40">
        <div className="max-w-6xl mx-auto flex items-center justify-between">
          <Link 
            href="/studio" 
            className="text-[#171719] text-sm uppercase tracking-wide hover:underline" 
            style={{ fontFamily: 'Neue Einstellung' }}
          >
            ← Back to Studio
          </Link>

          <nav 
            className="hidden md:flex items-center space-x-8 text-sm uppercase tracking-wide" 
            style={{ fontFamily: 'Neue Einstellung' }}
          >
            <Link href="/studio/editor" className="text-gray-600 hover:text-black hover:underline">Selfie Editor</Link>
            <Link href="/studio/feed-designer" className="text-gray-600 hover:text-black hover:underline">Feed Designer</Link>
            <Link href="/studio/photo-vault" className="text-black border-b-2 border-black pb-1">Photo Vault</Link>
          </nav>
        </div>
      </section>

      {/* Photo Vault */}
      <section className="py-24 px-4">
        <div className="max-w-7xl mx-auto text-center">
          <h1 
            className="text-5xl md:text-6xl uppercase tracking-wide mb-8" 
            style={{ fontFamily: 'Cormorant Garamond' }}
          >
            Photo Vault
          </h1>
          <div className="w-24 h-px mx-auto mb-8 bg-black" />
          <p 
            className="text-lg max-w-2xl mx-auto mb-16 leading-relaxed text-[#4C4B4B]" 
            style={{ fontFamily: 'Neue Einstellung', fontWeight: 300 }}
          >
            Your curated collection of brand visuals
          </p>

          {/* Filter Controls */}
          <div className="flex flex-wrap justify-center gap-6 mb-16">
            {["Filter by Date", "Filter by Tag", "Color Tone"].map((placeholder, i) => (
              <Select key={i}>
                <SelectTrigger 
                  className="w-48 border border-black uppercase tracking-wide" 
                  style={{ fontFamily: 'Neue Einstellung', fontSize: '14px', borderRadius: '0px' }}
                >
                  <SelectValue placeholder={placeholder} />
                </SelectTrigger>
                <SelectContent>
                  {placeholder === "Filter by Date" && (
                    <>
                      <SelectItem value="today">Today</SelectItem>
                      <SelectItem value="week">This Week</SelectItem>
                      <SelectItem value="month">This Month</SelectItem>
                    </>
                  )}
                  {placeholder === "Filter by Tag" && (
                    <>
                      <SelectItem value="selfie">Selfie</SelectItem>
                      <SelectItem value="edited">Edited</SelectItem>
                      <SelectItem value="posted">Posted</SelectItem>
                    </>
                  )}
                  {placeholder === "Color Tone" && (
                    <>
                      <SelectItem value="warm">Warm</SelectItem>
                      <SelectItem value="cool">Cool</SelectItem>
                      <SelectItem value="neutral">Neutral</SelectItem>
                    </>
                  )}
                </SelectContent>
              </Select>
            ))}
          </div>

          {/* Photo Grid */}
          <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-6 gap-4">
            {vaultImages.map((image, index) => (
              <div 
                key={index}
                className="aspect-square bg-white overflow-hidden cursor-pointer group"
              >
                <img 
                  src={image} 
                  alt={`Vault image ${index + 1}`}
                  className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                />
              </div>
            ))}
          </div>
        </div>
      </section>

      <Footer />
    </div>
  );
}
