URGENT: Fix AI Phase Content — Still Returning Summaries Instead of Full Detailed Paragraphs
📍PROBLEM:
The AI is still returning short summaries for phase_30, phase_60, and phase_90 — despite the recent prompt updates.

🔴 Current Output:
json
Kopier
Rediger
phase_30: "Story foundation & authentic voice clarity"
phase_60: "Content consistency & voice refinement"
phase_90: "Strategic growth & becoming the go-to expert"
🔴 Expected Output:
json
Kopier
Rediger
phase_30: "During the first 30 days, you’ll focus on uncovering and documenting your full transformation story. Start by mapping out key turning points, struggles, and breakthroughs. From there, begin creating your unique signature message—this becomes the foundation for your content pillars and your brand’s emotional connection."
🛠️ REQUIRED FIXES
✅ STEP 1: Fix the AI Prompt Logic (again) — Ensure It Overrides Fallback Defaults
In server/workbookAI.ts:

✅ Update prompt to ensure:

Each phase_30, phase_60, phase_90 is 3–4 sentences

Output must not default to template summaries

Output format returns a full string per phase, no fallback

ts
Kopier
Rediger
// Insert in prompt:
Return ONLY this format as a clean JSON:
{
  "phase_30": "3-4 detailed sentences describing what to build, fix, or focus on during the first 30 days.",
  "phase_60": "3-4 detailed sentences showing what is achieved, built, optimized by day 60.",
  "phase_90": "3-4 detailed sentences reflecting final outcomes, visibility growth, or scaling actions by day 90."
}

DO NOT return generic phrases. DO NOT summarize. Each phase must include a full paragraph, written in Sandra's emotionally engaging, motivational brand tone.
✅ Double check that no fallback values like "Story foundation..." are assigned anywhere else in your server logic.

✅ STEP 2: Force Logging for Verification
Add a temporary console log right after the AI call to log:

ts
Kopier
Rediger
console.log("AI-generated phase_30:", aiResponse.phase_30);
console.log("AI-generated phase_60:", aiResponse.phase_60);
console.log("AI-generated phase_90:", aiResponse.phase_90);
Use this to validate whether the AI is actually generating full content or not.

✅ STEP 3: STOP IF PHASE CONTENT IS EMPTY OR TOO SHORT
If any phase_xx is under 50 characters, block generation and return an error message to the user:

ts
Kopier
Rediger
if (
  !phase_30 || phase_30.length < 50 ||
  !phase_60 || phase_60.length < 50 ||
  !phase_90 || phase_90.length < 50
) {
  throw new Error("AI failed to generate full phase content. Please regenerate.");
}
This prevents broken PDFs and invalid previews.

✅ SUCCESS CHECKLIST
 AI returns full paragraph per phase

 No fallback summaries used anywhere

 PDF preview and download match

 Log output proves full content exists

 Workbook doesn’t generate if phase content is missing or too short

Let me know once this is implemented — I’ll retest everything!