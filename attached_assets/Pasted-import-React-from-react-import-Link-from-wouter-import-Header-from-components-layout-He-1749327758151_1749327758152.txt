import React from 'react';
import { Link } from "wouter";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";
import { Button } from "@/components/ui/button";
import { useQuery } from "@tanstack/react-query";

export default function FeedDesigner() {
  const { data: user } = useQuery({
    queryKey: ["/api/me"],
    retry: false,
  });

  return (
    <div className="min-h-screen bg-[#F1F1F1]">
      <Header user={user} />

      {/* Studio Navigation */}
      <section className="w-full py-4 px-4 border-b border-[#E5E5E5] sticky top-0 bg-white z-40">
        <div className="max-w-6xl mx-auto flex items-center justify-between">
          <Link 
            href="/studio" 
            className="text-[#171719] text-sm uppercase tracking-wide hover:underline" 
            style={{ fontFamily: 'Neue Einstellung' }}
          >
            ← Back to Studio
          </Link>

          <nav 
            className="hidden md:flex items-center space-x-8 text-sm uppercase tracking-wide" 
            style={{ fontFamily: 'Neue Einstellung' }}
          >
            <Link href="/studio/editor" className="text-gray-600 hover:text-black hover:underline">Selfie Editor</Link>
            <Link href="/studio/feed-designer" className="text-black border-b-2 border-black pb-1">Feed Designer</Link>
            <Link href="/studio/photo-vault" className="text-gray-600 hover:text-black hover:underline">Photo Vault</Link>
          </nav>
        </div>
      </section>

      {/* Feed Designer Content */}
      <section className="py-24 px-4">
        <div className="max-w-6xl mx-auto text-center">
          <h1 
            className="text-5xl md:text-6xl uppercase tracking-wide mb-8" 
            style={{ fontFamily: 'Cormorant Garamond' }}
          >
            Feed Designer
          </h1>
          <div className="w-24 h-px mx-auto mb-8 bg-black" />
          <p 
            className="text-lg max-w-2xl mx-auto mb-16 leading-relaxed text-[#4C4B4B]" 
            style={{ fontFamily: 'Neue Einstellung', fontWeight: 300 }}
          >
            Plan your Instagram grid with drag-and-drop functionality.
          </p>

          {/* Grid Planner */}
          <div className="grid grid-cols-3 gap-6 max-w-lg mx-auto mb-16">
            {Array.from({ length: 9 }, (_, i) => (
              <div 
                key={i} 
                className="aspect-square bg-white border border-dashed border-[#B5B5B3] flex items-center justify-center cursor-pointer hover:border-black transition-colors"
              >
                <span 
                  className="text-xs uppercase tracking-wide text-[#4C4B4B] hover:text-black" 
                  style={{ fontFamily: 'Neue Einstellung' }}
                >
                  Add Content
                </span>
              </div>
            ))}
          </div>

          {/* View Mode Buttons */}
          <div className="flex justify-center gap-4">
            <Button 
              className="uppercase border border-black text-black bg-transparent hover:bg-black hover:text-white transition-colors px-6 py-3" 
              style={{ fontFamily: 'Neue Einstellung', fontSize: '14px', letterSpacing: '0.5px', borderRadius: 0 }}
            >
              Grid View
            </Button>
            <Button 
              className="uppercase border border-black text-black bg-transparent hover:bg-black hover:text-white transition-colors px-6 py-3" 
              style={{ fontFamily: 'Neue Einstellung', fontSize: '14px', letterSpacing: '0.5px', borderRadius: 0 }}
            >
              Calendar View
            </Button>
          </div>
        </div>
      </section>

      <Footer />
    </div>
  );
}
