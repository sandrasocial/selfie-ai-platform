# LUXURY COURSE REDESIGN - IMPROVEMENTS NEEDED

The current implementation needs elevation to match luxury editorial standards. Please enhance with:

## 1. HERO VIDEO SECTION
Replace the current video embed with:
```css
.video-hero {
  position: relative;
  width: 100%;
  height: 100vh;
  max-height: 80vh;
  background: #000;
  overflow: hidden;
}

.video-wrapper {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
}

/* Add cinematic overlay gradient */
.video-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 40%;
  background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
}
2. TYPOGRAPHY HIERARCHY
Update all text styling:
css/* Hero Title */
.lesson-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(48px, 8vw, 96px);
  font-weight: 300;
  line-height: 1.1;
  letter-spacing: -0.03em;
  margin-bottom: 24px;
}

/* Module Indicator */
.module-indicator {
  font-family: 'Inter', sans-serif;
  font-size: 12px;
  font-weight: 300;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: #B5B5B3;
  margin-bottom: 32px;
}

/* Key sections with generous spacing */
.content-section {
  padding: 80px 0;
  max-width: 800px;
  margin: 0 auto;
}
3. NAVIGATION ENHANCEMENT
Transform the sidebar:

Add circular progress rings for each lesson
Smooth expand/collapse animations (0.4s cubic-bezier)
Hover states that elevate items
Visual completion indicators

4. AI TOOL CARDS
Replace basic cards with:
css.ai-tool-card {
  background: white;
  border: 1px solid rgba(181, 181, 179, 0.2);
  padding: 32px;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  cursor: pointer;
}

.ai-tool-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 24px 48px rgba(0, 0, 0, 0.08);
  border-color: #171719;
}

.ai-tool-icon {
  width: 48px;
  height: 48px;
  background: #F1F1F1;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 24px;
}
5. LESSON FLOW
Create distinct sections with:

Full-width alternating backgrounds (#F1F1F1 and white)
Centered content with max-width constraints
Visual separators between sections
Smooth scroll anchoring

6. INTERACTIONS TO ADD

Page load animation (fade in from bottom)
Smooth scroll behavior with easing
Progress bar that updates as user scrolls
Keyboard navigation (arrow keys)
Video chapter markers

7. MOBILE LUXURY

Full-screen video on mobile
Swipe gestures for navigation
Collapsible sections with smooth animations
Bottom sheet pattern for AI tools

Please implement these changes to create a truly editorial, magazine-quality learning experience that feels like a luxury brand.
