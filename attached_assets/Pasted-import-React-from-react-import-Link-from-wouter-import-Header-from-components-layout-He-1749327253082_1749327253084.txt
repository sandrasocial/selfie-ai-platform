import React from 'react';
import { Link } from "wouter";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { useQuery } from "@tanstack/react-query";

export default function BrandNotes() {
  const { data: user } = useQuery({
    queryKey: ["/api/me"],
    retry: false,
  });

  const notes = [
    {
      id: 1,
      title: "Brand Voice Guidelines",
      content: "Keep tone authentic, confident, and approachable. Use personal stories to connect.",
      date: "2024-01-15",
      image: "https://i.postimg.cc/HLN6LDSj/15.png"
    },
    {
      id: 2,
      title: "Color Palette Notes",
      content: "Stick to warm, natural tones. Avoid overly saturated filters.",
      date: "2024-01-14",
      image: "https://i.postimg.cc/Bbn4rXK7/3.png"
    }
  ];

  return (
    <div className="min-h-screen bg-[#F1F1F1]">
      <Header user={user} />
      
      {/* Navigation */}
      <section className="w-full py-4 px-4 border-b border-gray-100 sticky top-0 bg-white z-40">
        <div className="max-w-6xl mx-auto flex items-center justify-between">
          <Link href="/studio" className="text-[#171719] text-sm uppercase tracking-wide" style={{ fontFamily: 'Neue Einstellung' }}>
            ← Back to Studio
          </Link>
          <nav className="hidden md:flex items-center space-x-8 text-sm uppercase tracking-wide" style={{ fontFamily: 'Neue Einstellung' }}>
            <Link href="/studio/boost" className="text-gray-600 hover:text-black">Daily Boost</Link>
            <Link href="/studio/notes" className="text-black border-b-2 border-black pb-1">Brand Notes</Link>
            <Link href="/studio/tagger" className="text-gray-600 hover:text-black">Auto Tagger</Link>
          </nav>
        </div>
      </section>

      {/* Content */}
      <section className="py-20 px-4">
        <div className="max-w-6xl mx-auto">
          <div className="text-center mb-16">
            <h1 className="text-6xl font-normal uppercase tracking-wide mb-6" style={{ fontFamily: 'Cormorant Garamond' }}>
              Brand Notes
            </h1>
            <div className="w-24 h-px bg-black mx-auto mb-8"></div>
            <p className="text-lg max-w-2xl mx-auto" style={{ fontFamily: 'Neue Einstellung', fontWeight: 300 }}>
              Notion-style notes for your personal brand strategy.
            </p>
          </div>

          <div className="flex justify-between items-center mb-8">
            <h2 className="text-2xl uppercase tracking-wide" style={{ fontFamily: 'Cormorant Garamond' }}>Your Notes</h2>
            <Button className="uppercase border border-black text-black px-6 py-2 tracking-wide bg-transparent hover:bg-black hover:text-white transition" style={{ fontFamily: 'Neue Einstellung' }}>
              + New Note
            </Button>
          </div>

          <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
            {notes.map((note) => (
              <Card key={note.id} className="bg-white border border-[#B5B5B3] shadow hover:shadow-md transition">
                <div className="aspect-[4/3] overflow-hidden">
                  <img 
                    src={note.image} 
                    alt={note.title}
                    className="w-full h-full object-cover hover:scale-105 transition-transform duration-300"
                  />
                </div>
                <CardHeader>
                  <CardTitle className="text-lg uppercase tracking-wide" style={{ fontFamily: 'Cormorant Garamond' }}>
                    {note.title}
                  </CardTitle>
                </CardHeader>
                <CardContent>
                  <p className="text-sm text-[#4C4B4B] mb-4" style={{ fontFamily: 'Neue Einstellung' }}>
                    {note.content}
                  </p>
                  <div className="text-xs text-[#B5B5B3]" style={{ fontFamily: 'Neue Einstellung' }}>
                    {note.date}
                  </div>
                </CardContent>
              </Card>
            ))}

            {/* Add Note Card */}
            <Card className="border-2 border-dashed border-[#B5B5B3] bg-white text-center hover:border-black cursor-pointer transition">
              <CardContent className="flex flex-col justify-center items-center h-full py-16">
                <p className="uppercase text-sm tracking-wide text-[#4C4B4B]" style={{ fontFamily: 'Neue Einstellung' }}>
                  + Add New Note
                </p>
              </CardContent>
            </Card>
          </div>
        </div>
      </section>

      <Footer />
    </div>
  );
}
