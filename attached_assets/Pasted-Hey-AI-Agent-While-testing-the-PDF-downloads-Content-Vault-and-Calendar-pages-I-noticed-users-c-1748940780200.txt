Hey AI Agent,

While testing the PDF downloads, Content Vault, and Calendar pages, I noticed users currently have to manually copy and paste their generated content into the calendar. Let’s make this flow seamless. Here’s what I need you to implement:

---

## 1. Auto-Populate Calendar from Generated Content

1.1. **“Save to Calendar” Button**  
  • In `ContentVault.tsx`, add a “Save to Calendar” button next to each generated item.  
  • When clicked, it should automatically insert that content into the user’s calendar slot for its associated day—no manual copy/paste.  
  • Update the calendar data model (`blocks` or `calendar_entries` table) to accept contentText, dayNumber, and templateName directly from the selected vault item.

1.2. **Bulk Save from Templates**  
  • On the “Generate 90-Day Plan” flow, after AI generates content for Days 1–90, automatically create calendar entries for all those days with the generated hook, caption, and hashtags.  
  • Store them in the same `calendar_entries` table so the calendar grid displays them immediately.

1.3. **Drag & Drop Calendar Edits**  
  • Allow users to drag a content card from the vault onto the calendar cell if they want to move content to a different day.  
  • On drop, update the corresponding entry’s `dayNumber` and re-render the grid.

---

## 2. In-App Posting Strategy & Suggestions

2.1. **Personalized Posting Plan**  
  • Create a new endpoint `/api/strategize` that takes `user.id`, `user.niche`, and `user.skillLevel` (beginner/intermediate/expert).  
  • The agent should return a posting strategy JSON with:
    - **Frequency**: e.g., “Post 3x per week, increasing to 5x by Week 4.”  
    - **Content Mix**: “Use 2 template posts, 1 personal story post, and 1 educational post per week.”  
    - **Optimal Times**: Based on generic niche data—frontend returns recommended days/times.

2.2. **UI: Strategy Widget**  
  • In `Dashboard.tsx` or `Calendar.tsx`, add a “View My Posting Strategy” panel.  
  • When the user clicks it, call `/api/strategize` and display the returned plan in a simple card:  
    ```
    Beginner Plan
    • 3 posts per week (Mon, Wed, Fri at 11 AM)
    • Week 1: Use 2 templates + 1 story post
    • Week 2–4: Increase to 4 posts, add 1 educational video
    ```
  • Style this panel using Prata for headings and Helvetica Neue for body, with a Soft Beige background.

2.3. **Mixed Content Recommendations**  
  • After generating a 90-day plan, insert logic that “Every 7th day, replace that template post with a personalized strategy suggestion.” For instance, if Day 7 is a template hook, swap it with an AI prompt:  
    “Day 7: Share a quick tip about building confidence with your niche-specific insight.”  
  • Let users override these swaps manually if they prefer the original template.

---

## 3. One-Click Add from Strategy to Calendar

3.1. **“Add Strategy Post” Button**  
  • In the Strategy Widget, for each recommended post, add a “Add to Calendar” button.  
  • When clicked, it should create a `calendar_entries` record with the strategy text (not a template), auto-assign to that week’s date slot, and refresh the calendar view.

3.2. **Conflict Alerts**  
  • If a user tries to save strategy content on a day that already has a templated post, show a modal:  
    ```
    You already have a post on Day 14 (Template: “Your Niche-Driven Tip”).  
    Would you like to:
    [Replace] [Save to Day 15]
    ```
  • Handle user choice by updating or shifting the entry.

---

## 4. Backend & Data Model Updates

4.1. **calendar_entries Table**  
  • Fields: `id`, `user_id`, `day_number` (1–90), `content_text`, `template_name` (nullable), `is_strategy` (boolean), `created_at`, `updated_at`.  
  • Migrate existing calendar blocks to this new schema if needed.

4.2. **Strategy Endpoint Logic**  
  • In `server/strategyService.ts`, implement `generateStrategy(userId)`:
    - Fetch `user.niche` and `user.skillLevel` from the profile.  
    - Use AI to build a JSON strategy plan (frequency, content mix, times).  
    - Return that JSON to the frontend.

4.3. **Routes in server/routes.ts**  
  - `GET /api/strategize` → calls `generateStrategy(req.user.id)` and returns the plan.  
  - `POST /api/calendar/add` → expects `{ dayNumber, contentText, templateName?, isStrategy? }`, inserts into `calendar_entries`.

---

## 5. Frontend Integration & UX Flow

5.1. **Content Vault to Calendar Flow**  
  - When “Save to Calendar” is clicked in `ContentVault.tsx`, POST to `/api/calendar/add` with the selected item’s day, text, and templateName. Then refresh the Calendar view.

5.2. **Strategy Widget to Calendar Flow**  
  - In `Dashboard.tsx` (or a new `StrategyPanel.tsx`), after calling `/api/strategize`, render each plan item with a “Add to Calendar” button that POSTs to `/api/calendar/add` with `isStrategy: true`.

5.3. **Calendar View Updates**  
  - In `Calendar.tsx`, fetch `/api/calendar/entries?userId=` on mount.  
  - Render cells 1–90: if an entry exists for `day_number`, show its `content_text` (truncate to one line with “…” if too long).  
  - Use a small “S” badge if `isStrategy` = true, and a “T” badge if `template_name` is non-null.

---

## 6. Extra Suggestions to Add Value

6.1. **AI-Powered Content Reminders**  
  - If a user hasn’t added content for 3 consecutive calendar days, send a gentle nudge: “You’re due for Day 15 content—need a quick prompt?”  
  - Optionally, auto-schedule that prompt directly into the calendar if user says “Yes.”

6.2. **Weekly Summary Email**  
  - Every Sunday night, send an email summarizing:  
    - What they posted this week (dates + short preview)  
    - Next week’s strategy posts pending  
    - Engagement tips (e.g., “Try using this hashtag formula on Day 20.”)

6.3. **Mobile-Friendly “My Week” View**  
  - On small screens, collapse the 7-day calendar into a vertical “Today” and “Upcoming” list.  
  - Show “Today: Day 14 – [Snippet of content]” with a “Edit” button to swap strategy/template.

---

### To Summarize

- **Automate saving generated content** directly into the calendar via “Save to Calendar” buttons.  
- **Auto-populate** all 90 days with generated content so users don’t paste it manually.  
- **Add a “View Posting Strategy”** panel that calls a new `/api/strategize` endpoint to give a tailored plan (frequency, mix, times).  
- **Allow one-click “Add to Calendar”** from both templates and strategy items, resolving any conflicts if a day is already filled.  
- **Update calendar data model and API** to support `content_text`, `template_name`, and `isStrategy` flags.  
- **Polish the UX**: badges for strategy vs template, conflict modals, and a mobile-friendly “My Week” view.  
- **Bonus**: reminders for gaps, weekly summary emails, and small mobile enhancements.

Please implement these changes so the user journey is completely seamless, removes manual copy/paste, and guides them from beginner to expert with a tailored posting strategy. Let me know if you need any clarification or additional details!  
